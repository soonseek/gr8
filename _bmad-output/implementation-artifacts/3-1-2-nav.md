# Story 3-1-2-nav: 네비게이션 시스템 및 사용자 워크스페이스

Status: review
Priority: HIGH (Story 3-1의 Blocker 해결)

## Story

**As a** 사용자 (User),
**I want** 지갑 연결 후 직관적인 네비게이션과 워크스페이스 홈 화면을 보고 싶다,
**so that** 전략 개발, 백테스팅, 마켓플레이스 등 모든 gr8 서비스에 쉽게 접근할 수 있다.

---

## 배경 (Context)

**현재 상황:**
- Story 3-1에서 React Flow 에디터를 구현했지만, 접근 방법(라우팅)이 없음
- 지갑 연결 후 HomePage에 머물러 있음 (리다이렉션 로직 부재)
- 네비게이션 컴포넌트(사이드바, 헤더)가 없음
- 사용자를 위한 워크스페이스 홈 화면이 없음

**문제:**
- 사용자가 지갑 연결 후 어디로 가야 할지 알 수 없음
- 전략 에디터, 백테스팅, 마켓플레이스 등 주요 기능으로 접근할 수 없음
- Epic 3-8의 모든 기능을 탐색할 수 있는 UI 인프라 부족

**해결:**
1. 네비게이션 시스템 구축 (사이드바 + 헤더)
2. 사용자 워크스페이스 홈 화면 생성
3. 라우팅 구조 확장 및 인증 기반 리다이렉션

---

## 수용 기준 (Acceptance Criteria)

### AC 1: 네비게이션 사이드바 컴포넌트 생성

**Given** 사용자가 지갑 연결을 완료했을 때
**When** 메인 페이지가 로드되면
**Then** 좌측 사이드바 네비게이션이 표시된다
**And** 다음 메뉴 항목들이 포함된다:
  - 🏠 워크스페이스 (홈)
  - 📊 전략 개발 (에디터)
  - 📈 백테스팅
  - 🛒 마켓플레이스
  - 💰 수익 (크리에이터 보상)
  - ⚙️ 프로필/설정
**And** 각 메뉴 항목에 아이콘과 라벨이 표시된다
**And** 현재 페이지 경로가 활성(활성화된 메뉴 하이라이트) 상태로 표시된다
**And** 사이드바가 다크모드로 스타일링된다 (bg-gray-900)

### AC 2: 상단 헤더 컴포넌트 생성

**Given** 사용자가 인증된 상태일 때
**When** 모든 페이지에 접근하면
**Then** 상단 헤더가 표시된다
**And** 헤더에 다음 요소들이 포함된다:
  - 왼쪽: 햄버거 메뉴 버튼 (모바일에서 사이드바 토글)
  - 가운데: 페이지 타이틀
  - 오른쪽: 지갑 주소 표시 (짧게: 0x1234...5678)
  - 오른쪽: 로그아웃 버튼
**And** 헤더가 다크모드로 스타일링된다 (bg-gray-800, border-b)
**And** 모든 페이지에서 일관되게 표시된다

### AC 3: 사용자 워크스페이스 홈 화면 생성

**Given** 사용자가 지갑 연결을 완료했을 때
**When** `/workspace` 또는 `/` 경로로 접근하면
**Then** 워크스페이스 홈 화면이 표시된다
**And** 홈 화면에 다음 섹션들이 포함된다:
  - **헤로 섹션**: "환영합니다, {지갑 주소}" 메시지
  - **빠른 액션 카드**:
    - 새 전략 만들기 (버튼 → 에디터로 이동)
    - 백테스트 실행 (버튼 → 백테스트 페이지로 이동)
    - 마켓플레이스 탐색 (버튼 → 마켓플레이스로 이동)
  - **내 전략 섹션**: 최근 작성한 전략 목록 (최대 5개, Story 3-10 연동)
  - **최근 백테스트 섹션**: 최근 실행한 백테스트 목록 (최대 5개, Epic 4 연동)
  - **통계 카드**:
    - 총 전략 수
    - 실행한 백테스트 수
    - (선택사항) 보유 수익 (Epic 6 연동)
**And** 모든 섹션이 카드 형태로 디자인된다
**And** 다크모드 테마가 적용된다

### AC 4: 라우팅 구조 확장

**Given** 네비게이션 컴포넌트가 생성되었다
**When** 개발자가 라우팅을 설정하면
**Then** 다음 경로들이 정의된다:
  - `/` (또는 `/workspace`) → 워크스페이스 홈
  - `/editor` → 전략 개발 에디터 (Story 3-1)
  - `/backtest` → 백테스팅 페이지 (Epic 4)
  - `/marketplace` → 마켓플레이스 (Epic 5)
  - `/revenue` → 수익 대시보드 (Epic 6)
  - `/profile` → 프로필/설정
  - `/admin/*` → 관리자 대시보드 (기존 유지)
**And** 인증되지 않은 사용자는 `/` → HomePage (랜딩)로 접근한다
**And** 인증된 사용자는 `/` → `/workspace`로 리다이렉션된다

### AC 5: 인증 기반 리다이렉션 로직

**Given** 사용자가 지갑을 연결하면
**When** 연결이 성공적으로 완료되면
**Then** 자동으로 `/workspace`로 리다이렉션된다
**And** 브라우저 히스토리에 새 경로가 추가된다
**And** 네비게이션이 표시된다

**Given** 사용자가 이미 인증된 상태에서 `/`에 접근하면
**When** HomePage가 로드되면
**Then** 자동으로 `/workspace`로 리다이렉션된다

### AC 6: 반응형 네비게이션 디자인

**Given** 네비게이션이 구현되었다
**When** 개발자가 다양한 화면 크기에서 테스트하면
**Then** 모바일 (375px+):
  - 사이드바가 기본적으로 숨겨진다 (hidden)
  - 햄버거 메뉴 버튼으로 토GGLE된다 (슬라이드 인)
  - 사이드바 너비: 280px (열린 상태)
**And** 태블릿 (768px+):
  - 사이드바가 축소된 상태로 표시된다 (아이콘만)
  - 너비: 80px
**And** 데스크톱 (1024px+):
  - 사이드바가 항상 펼쳐진 상태로 표시된다
  - 너비: 250px
  - 아이콘 + 텍스트 모두 표시

### AC 7: 액티브 메뉴 하이라이트

**Given** 사용자가 네비게이션을 사용할 때
**When** 특정 페이지에 있으면
**Then** 해당 메뉴 항목이 하이라이트된다 (bg-gray-700)
**And** 활성 메뉴 아이템에 왼쪽 테두리가 표시된다 (border-l-4 border-blue-500)
**And** URL 경로와 메뉴 항목이 매핑된다

### AC 8: 레이아웃 컴포넌트 구조

**Given** 네비게이션과 헤더가 생성되었다
**When** 개발자가 레이아웃을 구성하면
**Then** `MainLayout` 컴포넌트가 생성된다
**And** `MainLayout`이 다음 구조를 갖는다:
  - 좌측: 사이드바 (조건부 렌더링)
  - 상단: 헤더 (고정)
  - 중앙: 페이지 컨텐츠 (스크롤 가능)
**And** 모든 페이지가 `MainLayout`으로 래핑된다
**And** `MainLayout`이 인증 상태를 확인하여 네비게이션 표시 여부를 결정한다

### AC 9: 빌드 및 타입 검증

**Given** 네비게이션 시스템이 구현되었다
**When** 개발자가 `npm run build`를 실행하면
**Then** 빌드가 성공적으로 완료된다
**And** TypeScript 타입 에러가 없다
**And** `npm run lint`가 통과한다

---

## Tasks / Subtasks

### Task 1: 네비게이션 사이드바 컴포넌트 생성 (AC: #1, #6, #7)
- [ ] Subtask 1.1: `src/components/layout/Sidebar.tsx` 생성
- [ ] Subtask 1.2: 메뉴 항목 타입 정의 (MenuItem 인터페이스)
- [ ] Subtask 1.3: 6개 메뉴 항목 렌더링 (홈, 전략 개발, 백테스팅, 마켓플레이스, 수익, 프로필)
- [ ] Subtask 1.4: 각 메뉴 항목에 아이콘 추가 (lucide-react 또는 heroicons)
- [ ] Subtask 1.5: 현재 경로 기반 활성 메뉴 하이라이트 구현
- [ ] Subtask 1.6: 다크모드 스타일링 (bg-gray-900, text-gray-100, hover:bg-gray-800)
- [ ] Subtask 1.7: 반응형 디자인 (모바일: hidden, 태블릿: 80px, 데스크톱: 250px)
- [ ] Subtask 1.8: 모바일에서 슬라이드 인/아웃 애니메이션 구현
- [ ] Subtask 1.9: 햄버거 메뉴 버튼으로 토GGLE 로직 구현

### Task 2: 상단 헤더 컴포넌트 생성 (AC: #2)
- [ ] Subtask 2.1: `src/components/layout/Header.tsx` 생성
- [ ] Subtask 2.2: 햄버거 메뉴 버튼 추가 (왼쪽)
- [ ] Subtask 2.3: 페이지 타이틀 표시 (가운데, 동적으로 변경)
- [ ] Subtask 2.4: 지갑 주소 표시 (오른쪽, useWallet 훅 활용)
- [ ] Subtask 2.5: 로그아웃 버튼 추가 (오른쪽, useDisconnect 훅 활용)
- [ ] Subtask 2.6: 다크모드 스타일링 (bg-gray-800, border-b border-gray-700)
- [ ] Subtask 2.7: 반응형 디자인 (모바일/태블릿/데스크톱)
- [ ] Subtask 2.8: 헤더 높이 고정 (h-16, 64px)

### Task 3: 사용자 워크스페이스 홈 페이지 생성 (AC: #3)
- [ ] Subtask 3.1: `src/pages/WorkspacePage.tsx` 생성
- [ ] Subtask 3.2: 헤로 섹션 구현 ("환영합니다, {지갑 주소}")
- [ ] Subtask 3.3: 빠른 액션 카드 3개 구현 (새 전략, 백테스트, 마켓플레이스)
- [ ] Subtask 3.4: "내 전략" 섹션 구현 (빈 상태 or Story 3-10 연동)
- [ ] Subtask 3.5: "최근 백테스트" 섹션 구현 (빈 상태 or Epic 4 연동)
- [ ] Subtask 3.6: 통계 카드 구현 (총 전략 수, 백테스트 수)
- [ ] Subtask 3.7: 카드 형태 디자인 (bg-gray-800, rounded-lg, p-6)
- [ ] Subtask 3.8: 그리드 레이아웃 (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)

### Task 4: 라우팅 구조 확장 (AC: #4, #5)
- [ ] Subtask 4.1: `src/App.tsx`에 Routes 추가
- [ ] Subtask 4.2: `/workspace` 경로에 WorkspacePage 연결
- [ ] Subtask 4.3: `/editor` 경로에 StrategyEditor 연결
- [ ] Subtask 4.4: `/backtest`, `/marketplace`, `/revenue`, `/profile` 경로 추가 (placeholder)
- [ ] Subtask 4.5: HomePage 컴포넌트 수정 (인증되지 않은 사용자용)
- [ ] Subtask 4.6: 인증된 사용자가 `/` 접근 시 `/workspace`로 리다이렉션
- [ ] Subtask 4.7: useAuth 훅으로 isAuthenticated 상태 확인
- [ ] Subtask 4.8: 지갑 연결 성공 후 `/workspace`로 리다이렉션 (WalletConnectionButton.tsx 수정)

### Task 5: MainLayout 컴포넌트 생성 (AC: #8)
- [ ] Subtask 5.1: `src/components/layout/MainLayout.tsx` 생성
- [ ] Subtask 5.2: Sidebar + Header + Content 영역 구조 구현
- [ ] Subtask 5.3: Outlet로 자식 라우트 렌더링 (react-router-dom v6)
- [ ] Subtask 5.4: 인증 상태 확인 (isAuthenticated)
- [ ] Subtask 5.5: 인증되지 않은 경우 네비게이션 숨김
- [ ] Subtask 5.6: 플렉스 레이아웃 구현 (flex-row, h-screen)

### Task 6: 네비게이션 스토어 생성 (선택사항)
- [ ] Subtask 6.1: `src/stores/navigationStore.ts` 생성 (Zustand)
- [ ] Subtask 6.2: 사이드바 열림/닫힘 상태 관리
- [ ] Subtask 6.3: 모바일에서 사이드바 토GGLE 액션 구현
- [ ] Subtask 6.4: localStorage에 사이드바 상태 저장 (선택사항)

### Task 7: 네비게이션 통합 및 테스트 (AC: #9)
- [ ] Subtask 7.1: App.tsx에 MainLayout 적용
- [ ] Subtask 7.2: 모든 페이지에 네비게이션 표시 확인
- [ ] Subtask 7.3: 모바일/태블릿/데스크톱 반응형 테스트
- [ ] Subtask 7.4: 메뉴 클릭 시 페이지 이동 테스트
- [ ] Subtask 7.5: 지갑 연결 후 리다이렉션 테스트
- [ ] Subtask 7.6: `npm run build` 실행
- [ ] Subtask 7.7: TypeScript 타입 에러 확인
- [ ] Subtask 7.8: `npm run lint` 실행

---

## Dev Notes

### 🎯 목표

이 Story는 **gr8 앱의 네비게이션 인프라**를 구축하는 것입니다. 완료되면 사용자가 지갑 연결 후 직관적인 UI로 모든 서비스(전략 개발, 백테스팅, 마켓플레이스 등)에 접근할 수 있게 됩니다. Story 3-1(React Flow 에디터)의 접근 가능성 문제를 해결하는 핵심 Story입니다.

### 📚 관련 아키텍처 패턴 및 제약사항

**React Router v6** [Source: architecture.md#Routing-Strategy]:
- `Routes`, `Route`, `Outlet`, `Navigate` 사용
- 중첩 라우팅 (MainLayout → 페이지 컴포넌트)
- 인증 기반 라우트 보호

**Tailwind CSS** [Source: architecture.md#Styling]:
- 유틸리티 퍼스트 스타일링
- 다크모드 테마 (bg-gray-900, text-gray-100)
- 반응형 클래스 (md:, lg:, xl:)
- Flexbox 및 Grid 레이아웃

**상태 관리** [Source: architecture.md#State-Management]:
- Zustand store (navigationStore.ts 선택사항)
- Context API (인증 상태는 이미 AuthContext로 구현됨)

**의존성 버전**:
```json
{
  "react-router-dom": "^7.12.0",  // 이미 설치됨
  "zustand": "^4.5.0",              // 이미 설치됨
  "lucide-react": "^0.263.1" 또는 "heroicons": "^2.0.0"  // 아이콘 라이브러리 (선택사항)
}
```

### 🏗️ 프로젝트 구조

**Story 3-1-nav에서 생성할 파일**:
```
src/
├── components/
│   └── layout/
│       ├── MainLayout.tsx        # ✅ 새로 생성 (메인 레이아웃)
│       ├── Sidebar.tsx           # ✅ 새로 생성 (좌측 사이드바)
│       ├── Header.tsx            # ✅ 새로 생성 (상단 헤더)
│       └── index.ts              # ✅ 새로 생성 (export)
├── pages/
│   ├── WorkspacePage.tsx         # ✅ 새로 생성 (워크스페이스 홈)
│   ├── BacktestPage.tsx          # ✅ 새로 생성 (placeholder)
│   ├── MarketplacePage.tsx       # ✅ 새로 생성 (placeholder)
│   ├── RevenuePage.tsx           # ✅ 새로 생성 (placeholder)
│   ├── ProfilePage.tsx           # ✅ 새로 생성 (placeholder)
│   └── index.ts
├── stores/
│   └── navigationStore.ts        # ✅ 새로 생성 (선택사항, Zustand)
└── App.tsx                       # ✅ 수정 (라우팅 구조 확장)
```

### ⚠️ Critical UX Considerations

**네비게이션 정보 아키텍처:**
- **홈/워크스페이스**: 사용자의 허브, 대시보드 역할
- **전략 개발**: 핵심 기능 (Story 3-1 에디터)
- **백테스팅**: 전략 검증 (Epic 4)
- **마켓플레이스**: 전략 발견 및 복제 (Epic 5)
- **수익**: 크리에이터 보상 (Epic 6)
- **프로필**: 사용자 설정

**인증 흐름:**
```
미인증 사용자:
  / → HomePage (랜딩, 지갑 연결 버튼)
       ↓ (지갑 연결 성공)
  /workspace → WorkspacePage (로그인 후 첫 화면)

인증된 사용자:
  / → 자동 리다이렉션 → /workspace
  /workspace → WorkspacePage
  /editor → StrategyEditor (Story 3-1)
  /backtest → BacktestPage (Epic 4)
  /marketplace → MarketplacePage (Epic 5)
  /revenue → RevenuePage (Epic 6)
  /profile → ProfilePage
```

**관리자 네비게이션:**
```
/admin/* → 기존 AdminDashboard 및 UserManagementPage 유지
          별도의 관리자 네비게이션 사용
```

### 🔧 코드 예시

**src/components/layout/MainLayout.tsx:**
```typescript
import { Outlet, Navigate } from 'react-router-dom';
import { Sidebar } from './Sidebar';
import { Header } from './Header';
import { useAuthContext } from '@/contexts/AuthContext';

export function MainLayout() {
  const { isAuthenticated } = useAuthContext();

  if (!isAuthenticated) {
    return <Navigate to="/" replace />;
  }

  return (
    <div className="flex h-screen bg-gray-900">
      <Sidebar />
      <div className="flex-1 flex flex-col">
        <Header />
        <main className="flex-1 overflow-auto">
          <Outlet />
        </main>
      </div>
    </div>
  );
}
```

**src/components/layout/Sidebar.tsx:**
```typescript
import { NavLink } from 'react-router-dom';
import { Home, TrendingUp, LineChart, ShoppingBag, DollarSign, Settings } from 'lucide-react';

const menuItems = [
  { path: '/workspace', label: '워크스페이스', icon: Home },
  { path: '/editor', label: '전략 개발', icon: TrendingUp },
  { path: '/backtest', label: '백테스팅', icon: LineChart },
  { path: '/marketplace', label: '마켓플레이스', icon: ShoppingBag },
  { path: '/revenue', label: '수익', icon: DollarSign },
  { path: '/profile', label: '프로필', icon: Settings },
];

export function Sidebar() {
  return (
    <aside className="hidden md:block w-64 bg-gray-900 border-r border-gray-800">
      <nav className="p-4">
        {menuItems.map((item) => (
          <NavLink
            key={item.path}
            to={item.path}
            className={({ isActive }) =>
              `flex items-center gap-3 px-4 py-3 rounded-lg mb-1 transition-colors ${
                isActive
                  ? 'bg-gray-800 text-blue-400 border-l-4 border-blue-500'
                  : 'text-gray-400 hover:bg-gray-800 hover:text-gray-100'
              }`
            }
          >
            <item.icon size={20} />
            <span>{item.label}</span>
          </NavLink>
        ))}
      </nav>
    </aside>
  );
}
```

**src/pages/WorkspacePage.tsx:**
```typescript
import { useNavigate } from 'react-router-dom';
import { useAuthContext } from '@/contexts/AuthContext';
import { Plus, Play, Compass } from 'lucide-react';

export function WorkspacePage() {
  const { address } = useAuthContext();
  const navigate = useNavigate();

  return (
    <div className="p-6 max-w-7xl mx-auto">
      {/* Hero Section */}
      <div className="mb-8">
        <h1 className="text-3xl font-bold text-gray-100">
          환영합니다, {address?.slice(0, 6)}...{address?.slice(-4)}!
        </h1>
        <p className="text-gray-400 mt-2">전략을 개발하고 백테스트하세요</p>
      </div>

      {/* Quick Actions */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <button
          onClick={() => navigate('/editor')}
          className="bg-gray-800 hover:bg-gray-700 rounded-lg p-6 text-left transition-colors"
        >
          <Plus className="text-blue-400 mb-4" size={32} />
          <h3 className="text-lg font-semibold text-gray-100 mb-2">새 전략 만들기</h3>
          <p className="text-gray-400 text-sm">노드 기반 에디터로 전략을 구성하세요</p>
        </button>

        <button
          onClick={() => navigate('/backtest')}
          className="bg-gray-800 hover:bg-gray-700 rounded-lg p-6 text-left transition-colors"
        >
          <Play className="text-green-400 mb-4" size={32} />
          <h3 className="text-lg font-semibold text-gray-100 mb-2">백테스트 실행</h3>
          <p className="text-gray-400 text-sm">전략을 과거 데이터로 테스트하세요</p>
        </button>

        <button
          onClick={() => navigate('/marketplace')}
          className="bg-gray-800 hover:bg-gray-700 rounded-lg p-6 text-left transition-colors"
        >
          <Compass className="text-purple-400 mb-4" size={32} />
          <h3 className="text-lg font-semibold text-gray-100 mb-2">마켓플레이스</h3>
          <p className="text-gray-400 text-sm">다른 사용자의 전략을 탐색하세요</p>
        </button>
      </div>

      {/* My Strategies (placeholder for Story 3-10) */}
      <div className="bg-gray-800 rounded-lg p-6 mb-6">
        <h2 className="text-xl font-semibold text-gray-100 mb-4">내 전략</h2>
        <p className="text-gray-400">아직 전략이 없습니다. 첫 번째 전략을 만들어보세요!</p>
      </div>

      {/* Recent Backtests (placeholder for Epic 4) */}
      <div className="bg-gray-800 rounded-lg p-6">
        <h2 className="text-xl font-semibold text-gray-100 mb-4">최근 백테스트</h2>
        <p className="text-gray-400">아직 백테스트를 실행하지 않았습니다.</p>
      </div>
    </div>
  );
}
```

**src/App.tsx (수정):**
```typescript
import { Routes, Route, Navigate } from 'react-router-dom';
import { MainLayout } from './components/layout';
import { HomePage } from './pages/HomePage';
import { WorkspacePage } from './pages/WorkspacePage';
import { StrategyEditor } from './components/editor/StrategyEditor';
import { AdminDashboard } from './pages/AdminDashboard';
import { UserManagementPage } from './pages/UserManagementPage';
// ... other imports

function App() {
  return (
    <AuthProvider>
      <SessionExpiredAlert />
      <Toaster position="top-center" />
      <Routes>
        {/* Public routes */}
        <Route path="/" element={<HomePage />} />

        {/* Protected routes (with navigation) */}
        <Route element={<MainLayout />}>
          <Route path="/workspace" element={<WorkspacePage />} />
          <Route path="/editor" element={<StrategyEditor />} />
          <Route path="/backtest" element={<div>백테스트 페이지 (Epic 4)</div>} />
          <Route path="/marketplace" element={<div>마켓플레이스 (Epic 5)</div>} />
          <Route path="/revenue" element={<div>수익 대시보드 (Epic 6)</div>} />
          <Route path="/profile" element={<div>프로필/설정</div>} />
        </Route>

        {/* Admin routes (separate navigation) */}
        <Route path="/admin" element={<AdminDashboard />} />
        <Route path="/admin/users" element={<UserManagementPage />} />
      </Routes>
    </AuthProvider>
  );
}
```

### 📐 레이아웃 구조

**데스크톱 레이아웃 (1024px+):**
```
┌────────────┬────────────────────────────────────┐
│            │  Header (64px)                    │
│   Sidebar  ├────────────────────────────────────┤
│  (250px)   │                                    │
│            │  Page Content                      │
│  🏠 Home   │  (scrollable)                      │
│  📊 Editor │                                    │
│  📈 Back   │                                    │
│  🛒 Market │                                    │
│  💰 Rev    │                                    │
│  ⚙️ Profile│                                    │
│            │                                    │
└────────────┴────────────────────────────────────┘
```

**모바일 레이아웃 (375px+):**
```
┌──────────────────────────────┐
│ ☰  Header (64px)        👤   │
├──────────────────────────────┤
│                              │
│                              │
│  Page Content                │
│  (scrollable)                │
│                              │
│                              │
└──────────────────────────────┘

Sidebar: 슬라이드 인 (overlay)
```

### 🎨 스타일링 가이드

**다크모드 색상:**
- 배경: `bg-gray-900`, `bg-gray-800` (카드)
- 텍스트: `text-gray-100` (주요), `text-gray-400` (보조)
- 보더: `border-gray-800`, `border-gray-700`
- 활성 메뉴: `bg-gray-800`, `border-blue-500`

**반응형 네비게이션:**
- 모바일: `hidden md:flex` (사이드바 숨김)
- 태블릿: `w-20` (축소, 아이콘만)
- 데스크톱: `w-64` (펼쳐짐)

### 🔄 의존성 및 후속 작업

**의존 Stories:**
- ✅ Story 2-1~2-3: Web3 지갑 연동 (인증 상태 활용)
- ✅ Story 3-1: React Flow 에디터 (페이지 연결)

**후속 Stories:**
- Story 3-10: 전략 저장/로드 (WorkspacePage "내 전략" 섹션 연동)
- Epic 4: 백테스팅 (BacktestPage 구현)
- Epic 5: 마켓플레이스 (MarketplacePage 구현)
- Epic 6: 크리에이터 보상 (RevenuePage 구현)

### ⚡ 성능 최적화

**코드 스플리팅:**
- 페이지별 lazy loading (React.lazy)
```typescript
const BacktestPage = lazy(() => import('./pages/BacktestPage'));
const MarketplacePage = lazy(() => import('./pages/MarketplacePage'));
```

**네비게이션 최적화:**
- 사이드바 메뉴 아이템에 `React.memo` 적용
- 불필요한 리렌더링 방지

### 🐛 알려진 문제 및 해결 방안

**문제 1: 지갑 연결 후 리다이렉션 안 됨**
- 해결: WalletConnectionButton.tsx에서 연결 성공 후 `navigate('/workspace')` 호출

**문제 2: 인증 상태 확인 시점**
- 해결: AuthContext의 `isAuthenticated` 상태 사용
- PrivateRoute 컴포넌트로 라우트 보호

**문제 3: 모바일에서 사이드바 토GGLE**
- 해결: navigationStore.ts에 `isSidebarOpen` 상태 관리
- useEffect로 모바일에서 사이드바 닫기 (라우트 변경 시)

---

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5

### Debug Log References
None

### Completion Notes List

### File List

**Frontend Layout Components (3 files)**
- `gr8-frontend/src/components/layout/MainLayout.tsx` - 메인 레이아웃 (~80 lines)
- `gr8-frontend/src/components/layout/Sidebar.tsx` - 좌측 사이드바 (~120 lines)
- `gr8-frontend/src/components/layout/Header.tsx` - 상단 헤더 (~100 lines)

**Pages (5 files)**
- `gr8-frontend/src/pages/WorkspacePage.tsx` - 워크스페이스 홈 (~150 lines)
- `gr8-frontend/src/pages/BacktestPage.tsx` - 백테스트 페이지 (placeholder, ~20 lines)
- `gr8-frontend/src/pages/MarketplacePage.tsx` - 마켓플레이스 (placeholder, ~20 lines)
- `gr8-frontend/src/pages/RevenuePage.tsx` - 수익 대시보드 (placeholder, ~20 lines)
- `gr8-frontend/src/pages/ProfilePage.tsx` - 프로필/설정 (placeholder, ~20 lines)

**State Management (1 file, optional)**
- `gr8-frontend/src/stores/navigationStore.ts` - 네비게이션 상태 (~50 lines)

**Modified Files (2 files)**
- `gr8-frontend/src/App.tsx` - 라우팅 구조 확장 (~80 lines)
- `gr8-frontend/src/components/WalletConnectionButton.tsx` - 리다이렉션 로직 추가 (~10 lines 수정)

**Story Files (1 file)**
- `_bmad-output/implementation-artifacts/3-1-nav.md` - This story file

**Total:** 12 files created/modified
